#!/bin/bash
read -p "请您输入合法的ip地址，第1、4位不能以0开头，不能大于255不能小于0: " ip
echo $ip |grep -E -o \ 
"^(([1-9]{1,2}|1[0-9][0-9]|2[0-4][0-9]|25[0-5])\.){3}([1-9]{1,2}|1[0-9][0-9]|2[0-4][0-9]|25[0-5])$" &>/dev/null
if [ $? = 0 ]then;echo "您输入的ip是$ip!";else echo "请输入合法IP地址！";fi

